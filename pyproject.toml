[project]
name = "yabk-dump"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "pycookiecheat>=0.8.0",
    "pycryptodome>=3.23.0",
    "requests>=2.34.2",
]

[dependency-groups]
dev = [
    "ruff>=0.15.20",
]
